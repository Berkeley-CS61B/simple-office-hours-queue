generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["referentialIntegrity"]
}

datasource db {
    provider             = "mysql"
    url                  = env("DATABASE_URL")
    referentialIntegrity = "prisma"
}

// TODO add timeSpent
model Ticket {
    id           String       @id @default(cuid())
    createdBy    User         @relation(fields: [userId], references: [id])
    createdAt    DateTime     @default(now())
    updatedAt    DateTime     @default(now()) @updatedAt
    ticketStatus TicketStatus
    description  String?
    assignment   String
    location     String
    resolution   String

    userId  String
    queueId String
}

model Account {
    id                String  @id @default(cuid())
    userId            String
    type              String
    provider          String
    providerAccountId String
    refresh_token     String? @db.Text
    access_token      String? @db.Text
    expires_at        Int?
    token_type        String?
    scope             String?
    id_token          String? @db.Text
    session_state     String?
    user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@unique([provider, providerAccountId])
}

model User {
    id            String    @id @default(cuid())
    name          String?
    email         String?   @unique
    emailVerified DateTime?
    image         String?
    role          UserRole  @default(STUDENT)
    accounts      Account[]
    sessions      Session[]
    Ticket        Ticket[]
}

model Session {
    id           String   @id @default(cuid())
    sessionToken String   @unique
    userId       String
    expires      DateTime
    user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
    identifier String
    token      String   @unique
    expires    DateTime

    @@unique([identifier, token])
}

enum UserRole {
    STUDENT
    STAFF
    CONCIERGE
    ADMIN
}

enum TicketStatus {
    PENDING // Concierge stage
    ASSIGNED
    RESOLVED
    DELETED
}

// Schema for an office hours queue
// model User {
//     id      String    @id @default(cuid())
//     name    String
//     email   String?   @unique
//     role    UserRole
//     Ticket  Ticket[]
//     Queue   Queue? // Allows TAs to have personal queues
//     Message Message[]
// }

// model Ticket {
//     id           String       @id @default(cuid())
//     createdBy    User         @relation(fields: [userId], references: [id])
//     createdAt    DateTime     @default(now())
//     updatedAt    DateTime     @default(now()) @updatedAt
//     ticketStatus TicketStatus
//     description  String?
//     assignment   String
//     location     String
//     timeSpent    String // Better type?
//     Queue        Queue        @relation(fields: [queueId], references: [id])
//     chatMessages Message[]
//     resolution   String

//     userId  String
//     queueId String
// }

// model Message {
//     id        String   @id @default(cuid())
//     createdAt DateTime @default(now())
//     content   String
//     author    User     @relation(fields: [userId], references: [id])
//     ticket    Ticket   @relation(fields: [ticketId], references: [id])
//     userId    String
//     ticketId  String
// }

// model Queue {
//     id      String   @id @default(cuid())
//     owner   User?    @relation(fields: [userId], references: [id])
//     Tickets Ticket[]
//     userId  String?  @unique
// }

// enum TicketStatus {
//     PENDING // Concierge stage
//     ASSIGNED
//     RESOLVED
//     DELETED
// }

// enum UserRole {
//     STUDENT
//     STAFF
//     CONCIERGE
//         ADMIN
// }
